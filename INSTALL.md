./script/bootstrap
./script/install

* external
  - twilio
  - mailgun (for email parsing)

* dependencies
  - postgres
  - redis

* create .env file from sample
* create first user
* create a global oauth accounts:
  - one for users
  - one for services