Revily.ApplicationRoute = Ember.Route.extend Ember.SimpleAuth.ApplicationRouteMixin
  # model: ->
    # Revily.User.find('me')

#   # setupController: (controller, currentUser) ->
#     # @controllerFor('currentUser').set('model', 'currentUser')
